{
  "id": 1,
  "title": "Vue 3 Composition API 深度解析",
  "titleEn": "Deep Dive into Vue 3 Composition API",
  "coverImage": "https://images.unsplash.com/photo-1555066931-4365d14bab8c?w=800&q=80",
  "tags": ["frontend"],
  "date": "2024-03-15",
  "views": 2847,
  "author": "TechBlog",
  "excerpt": "深入探索 Vue 3 的 Composition API，了解如何利用组合式函数构建更加模块化和可复用的代码结构。",
  "excerptEn": "Explore Vue 3's Composition API in depth and learn how to build more modular and reusable code structures with composables.",
  "toc": [
    { "id": "what-is-composition-api", "text": "什么是 Composition API", "level": 2 },
    { "id": "setup-function", "text": "setup() 函数详解", "level": 2 },
    { "id": "reactive-system", "text": "响应式系统核心", "level": 2 },
    { "id": "computed-watch", "text": "计算属性与监听器", "level": 2 },
    { "id": "lifecycle-hooks", "text": "生命周期钩子", "level": 2 },
    { "id": "composables", "text": "组合式函数（Composables）", "level": 2 },
    { "id": "best-practices", "text": "最佳实践与注意事项", "level": 2 }
  ],
  "content": "<p>Vue 3 引入了全新的 Composition API，这是一种组织组件逻辑的新方式。相比于 Vue 2 中的 Options API，Composition API 提供了更灵活、更强大的代码组织能力，特别适合处理复杂的组件逻辑和实现代码复用。本文将深入探讨 Vue 3 Composition API 的各个方面，帮助开发者全面掌握这一强大的特性。</p><h2 id=\"what-is-composition-api\">什么是 Composition API</h2><p>Composition API 是 Vue 3 引入的一种全新的组件逻辑组织方式。它的核心思想是将相关的响应式状态和逻辑函数组合在一起，而不是按照选项类型（data、methods、computed 等）分散在不同的地方。这种方式被称为"逻辑关注点组合"（Logic Composition）。</p><p>传统的 Options API 要求开发者按照预定义的选项来组织代码，当组件变得复杂时，相关的逻辑可能会被分散到多个选项中，导致代码难以维护和理解。例如，一个处理用户交互、数据获取和表单验证的组件，可能需要跨越 data、methods、computed、watch 等多个选项来组织代码。</p><p>Composition API 通过提供一系列响应式 API 和生命周期钩子，让开发者能够按照逻辑功能来组织代码。同一个功能的代码可以放在一起，无论涉及的是响应式状态、方法还是计算属性。这种方式不仅使代码更易维护，还大大提高了代码的可复用性。</p><h2 id=\"setup-function\">setup() 函数详解</h2><p>setup() 是 Composition API 的入口点，它在组件实例创建之前、props 解析之后立即调用。在这个函数中，我们可以使用 Composition API 的所有功能。</p><pre><code>// 基础 setup 函数示例\nimport { ref, reactive } from 'vue'\n\nexport default {\n  name: 'UserProfile',\n  props: {\n    userId: {\n      type: Number,\n      required: true\n    }\n  },\n  setup(props) {\n    // 使用 ref 创建响应式引用\n    const count = ref(0)\n    const message = ref('Hello Vue 3')\n    \n    // 使用 reactive 创建响应式对象\n    const state = reactive({\n      name: '张三',\n      email: 'zhangsan@example.com',\n      isLoading: false\n    })\n    \n    // setup 返回的对象属性和方法可以在模板中使用\n    return {\n      count,\n      message,\n      state\n    }\n  }\n}\n</code></pre><p>setup() 函数接收两个参数：props 和 context。props 包含了组件接收的所有属性，它是响应式的，当父组件传递的 props 变化时会自动更新。context 则提供了组件实例的其他属性和方法，包括 attrs、slots、emit 等。</p><p>需要特别注意的是，在 setup() 函数内部，<strong>this</strong> 关键字不会指向组件实例。这是因为 setup() 在组件实例创建之前执行，所以无法访问 data、computed、methods 等选项中定义的属性。我们应该使用新的 API 来替代原本使用 this 的场景。</p><h2 id=\"reactive-system\">响应式系统核心</h2><p>Vue 3 的响应式系统是 Composition API 的基础。理解响应式系统的工作原理对于熟练使用 Composition API 至关重要。Vue 3 提供了两种创建响应式数据的方式：ref 和 reactive。</p><h3>ref() 函数</h3><p>ref() 用于创建包含单一值的响应式引用。它接受一个内部值作为参数，返回一个响应式的对象，这个对象包含一个 .value 属性。</p><pre><code>import { ref } from 'vue'\n\n// 创建响应式引用\nconst count = ref(0)\nconst name = ref('张三')\nconst isActive = ref(true)\n\n// 访问和修改值\nconsole.log(count.value) // 0\ncount.value++\nconsole.log(count.value) // 1\n\n// 在模板中会自动解包，不需要使用 .value\n</code></pre><p>ref() 函数特别适合处理原始数据类型，如数字、字符串、布尔值等。当在模板中使用时，Vue 会自动解包 ref，无需手动添加 .value。但在 JavaScript 中访问时，仍然需要使用 .value 来获取或设置值。</p><h3>reactive() 函数</h3><p>reactive() 用于创建响应式对象，它会将一个普通对象转换为深度响应式对象。与 ref() 不同，reactive() 不需要通过 .value 来访问数据。</p><pre><code>import { reactive } from 'vue'\n\n// 创建响应式对象\nconst user = reactive({\n  name: '张三',\n  age: 25,\n  address: {\n    city: '北京',\n    district: '朝阳区'\n  }\n})\n\n// 直接访问和修改\nconsole.log(user.name) // '张三'\nuser.age = 26\nuser.address.city = '上海'\n\n// 不能直接替换整个对象\n// user = reactive({ name: '李四' }) // 错误！\n\n// 应该使用 Object.assign 或解构后重新赋值\nObject.assign(user, { name: '李四' })\n</code></pre><p>reactive() 的一个重要特性是它会深度监听对象的所有嵌套属性，任何层级的变化都会被追踪到。但这也可能带来性能问题，对于大型数据结构需要谨慎使用。</p><h3>toRef() 和 toRefs()</h3><p>有时候我们需要将 reactive 对象的属性转换为 ref，以便在保持响应式的同时进行解构操作。Vue 提供了 toRef() 和 toRefs() 两个工具函数来处理这种情况。</p><pre><code>import { reactive, toRef, toRefs } from 'vue'\n\nconst state = reactive({\n  count: 0,\n  message: 'Hello'\n})\n\n// 使用 toRefs 转换整个对象\nconst { count, message } = toRefs(state)\n\n// 现在 count 和 message 都是 ref\ncount.value++\nmessage.value = 'World'\n\n// 使用 toRef 转换单个属性\nconst countRef = toRef(state, 'count')\nconsole.log(countRef.value) // 1\n</code></pre><h2 id=\"computed-watch\">计算属性与监听器</h2><p>在 Composition API 中，计算属性和监听器分别通过 computed() 和 watch/watchEffect() 函数来实现。这些函数提供了与 Options API 中相同的功能，但使用方式更加灵活。</p><h3>computed() 计算属性</h3><p>computed() 函数用于创建计算属性。它接收一个 getter 函数作为参数，返回一个只读的响应式 ref 对象。</p><pre><code>import { ref, computed } from 'vue'\n\nconst firstName = ref('张')\nconst lastName = ref('三')\n\n// 只读计算属性\nconst fullName = computed(() => {\n  return lastName.value + firstName.value\n})\n\n// 带 get 和 set 的计算属性\nconst fullNameWritable = computed({\n  get() {\n    return lastName.value + firstName.value\n  },\n  set(newValue) {\n    const len = newValue.length\n    if (len >= 2) {\n      lastName.value = newValue.slice(0, 1)\n      firstName.value = newValue.slice(1)\n    }\n  }\n})\n\nconsole.log(fullName.value) // '张三'\nfullNameWritable.value = '李四'\nconsole.log(firstName.value) // '四'\nconsole.log(lastName.value) // '李'\n</code></pre><h3>watch() 监听器</h3><p>watch() 函数用于监听响应式数据的变化，并执行相应的回调函数。它与 Options API 中的 watch 选项功能相同，但提供了更多的配置选项。</p><pre><code>import { ref, watch } from 'vue'\n\nconst count = ref(0)\nconst user = ref({ name: '张三', age: 25 })\n\n// 监听单个 ref\nwatch(count, (newValue, oldValue) => {\n  console.log(`count 从 ${oldValue} 变为 ${newValue}`)\n})\n\n// 监听多个数据源\nwatch([count, user], ([newCount, newUser], [oldCount, oldUser]) => {\n  console.log('数据变化了')\n}, { deep: true })\n\n// 深度监听对象\nwatch(user, (newUser) => {\n  console.log('用户信息更新:', newUser)\n}, { deep: true })\n\n// 立即执行\nwatch(user, (newUser) => {\n  console.log('初始用户信息:', newUser)\n}, { immediate: true })\n</code></pre><h3>watchEffect() 立即执行监听</h3><p>watchEffect() 是一个更便捷的监听方式，它会在初始化时立即执行一次回调，并在回调中访问的响应式数据变化时再次执行。</p><pre><code>import { ref, watchEffect } from 'vue'\n\nconst count = ref(0)\nconst name = ref('张三')\n\n// 立即执行，追踪 count 和 name\nwatchEffect(() => {\n  console.log(`用户 ${name.value} 的计数: ${count.value}`)\n})\n\n// 清理副作用\nwatchEffect((onInvalidate) => {\n  const timer = setTimeout(() => {\n    console.log('延迟操作完成')\n  }, 1000)\n  \n  // 清理函数\n  onInvalidate(() => {\n    clearTimeout(timer)\n  })\n})\n</code></pre><h2 id=\"lifecycle-hooks\">生命周期钩子</h2><p>Composition API 提供了与 Options API 对应的生命周期钩子函数。这些函数需要从 vue 包中导入，并在 setup() 函数内部调用。</p><pre><code>import { \n  onMounted, \n  onUpdated, \n  onUnmounted,\n  onBeforeMount,\n  onBeforeUpdate,\n  onBeforeUnmount,\n  onErrorCaptured,\n  onRenderTracked,\n  onRenderTriggered\n} from 'vue'\n\nexport default {\n  setup() {\n    // beforeCreate 和 created 需要使用 setup 本身代替\n    \n    onBeforeMount(() => {\n      console.log('组件即将挂载')\n    })\n    \n    onMounted(() => {\n      console.log('组件已挂载')\n      // 适合执行 DOM 操作\n      // 适合添加事件监听器\n    })\n    \n    onBeforeUpdate(() => {\n      console.log('组件即将更新')\n    })\n    \n    onUpdated(() => {\n      console.log('组件已更新')\n      // 适合执行基于更新后的 DOM 的操作\n    })\n    \n    onBeforeUnmount(() => {\n      console.log('组件即将卸载')\n      // 适合清理副作用\n      // 取消定时器\n      // 移除事件监听器\n    })\n    \n    onUnmounted(() => {\n      console.log('组件已卸载')\n    })\n    \n    onErrorCaptured((err, instance, info) => {\n      console.error('捕获到错误:', err)\n      console.error('错误信息:', info)\n      return false // 阻止错误继续传播\n    })\n    \n    return {}\n  }\n}\n</code></pre><p>需要注意的是，setup() 函数本身相当于 beforeCreate 和 created 钩子。如果需要在这些阶段执行代码，直接在 setup() 函数中添加即可。另外，新增的 onRenderTracked 和 onRenderTriggered 是调试相关的钩子，它们在开发环境下可以帮助我们追踪组件的渲染依赖和触发源。</p><h2 id=\"composables\">组合式函数（Composables）</h2><p>组合式函数是 Composition API 最强大的特性之一。它是一种封装和复用逻辑的模式，类似于 React 中的自定义 Hooks。一个组合式函数是一个使用 Composition API 封装的、可复用的功能单元。</p><h3>鼠标位置追踪</h3><p>让我们从一个简单的例子开始，创建一个追踪鼠标位置的组合式函数。</p><pre><code>// useMousePosition.js\nimport { ref, onMounted, onUnmounted } from 'vue'\n\nexport function useMousePosition() {\n  const x = ref(0)\n  const y = ref(0)\n  \n  const updatePosition = (event) => {\n    x.value = event.clientX\n    y.value = event.clientY\n  }\n  \n  onMounted(() => {\n    window.addEventListener('mousemove', updatePosition)\n  })\n  \n  onUnmounted(() => {\n    window.removeEventListener('mousemove', updatePosition)\n  })\n  \n  return { x, y }\n}\n</code></pre><p>在组件中使用这个组合式函数：</p><pre><code>&lt;template&gt;\n  &lt;div&gt;\n    &lt;p&gt;鼠标位置: {{ x }}, {{ y }}&lt;/p&gt;\n  &lt;/div&gt;\n&lt;/template&gt;\n\n&lt;script setup&gt;\nimport { useMousePosition } from './useMousePosition'\n\nconst { x, y } = useMousePosition()\n&lt;/script&gt;\n</code></pre><h3>异步数据获取</h3><p>组合式函数也可以用于封装数据获取逻辑：</p><pre><code>// useFetch.js\nimport { ref, watchEffect } from 'vue'\n\nexport function useFetch(url) {\n  const data = ref(null)\n  const error = ref(null)\n  const isLoading = ref(true)\n  \n  const fetchData = async () => {\n    try {\n      isLoading.value = true\n      const response = await fetch(url)\n      if (!response.ok) {\n        throw new Error(`HTTP error! status: ${response.status}`)\n      }\n      data.value = await response.json()\n      error.value = null\n    } catch (err) {\n      error.value = err.message\n      data.value = null\n    } finally {\n      isLoading.value = false\n    }\n  }\n  \n  watchEffect(() => {\n    fetchData()\n  })\n  \n  const refresh = () => {\n    fetchData()\n  }\n  \n  return { data, error, isLoading, refresh }\n}\n</code></pre><h3>表单验证</h3><p>组合式函数在表单验证场景中也大有可为：</p><pre><code>// useFormValidation.js\nimport { ref, computed, reactive } from 'vue'\n\nexport function useFormValidation(initialValues = {}) {\n  const values = reactive({ ...initialValues })\n  const errors = reactive({})\n  const touched = reactive({})\n  \n  const rules = {}\n  \n  const setRules = (newRules) => {\n    Object.assign(rules, newRules)\n  }\n  \n  const validate = (fieldName) => {\n    const fieldRules = rules[fieldName]\n    if (!fieldRules) return true\n    \n    const value = values[fieldName]\n    for (const rule of fieldRules) {\n      const result = rule(value)\n      if (result !== true) {\n        errors[fieldName] = result\n        return false\n      }\n    }\n    errors[fieldName] = null\n    return true\n  }\n  \n  const validateAll = () => {\n    let isValid = true\n    for (const fieldName in rules) {\n      if (!validate(fieldName)) {\n        isValid = false\n      }\n    }\n    return isValid\n  }\n  \n  const handleBlur = (fieldName) => {\n    touched[fieldName] = true\n    validate(fieldName)\n  }\n  \n  const reset = () => {\n    Object.keys(values).forEach(key => {\n      values[key] = initialValues[key] || ''\n      errors[key] = null\n      touched[key] = false\n    })\n  }\n  \n  const isValid = computed(() => {\n    return Object.values(errors).every(e => e === null)\n  })\n  \n  return {\n    values,\n    errors,\n    touched,\n    isValid,\n    setRules,\n    validate,\n    validateAll,\n    handleBlur,\n    reset\n  }\n}\n\n// 使用示例\n// const { values, errors, validateAll, handleBlur } = useFormValidation({\n//   email: '',\n//   password: ''\n// })\n// \n// setRules({\n//   email: [\n//     v => !!v || '邮箱不能为空',\n//     v => /.+@.+/.test(v) || '邮箱格式不正确'\n//   ],\n//   password: [\n//     v => !!v || '密码不能为空',\n//     v => v.length >= 6 || '密码至少6位'\n//   ]\n// })\n</code></pre><h2 id=\"best-practices\">最佳实践与注意事项</h2><p>在使用 Composition API 时，遵循一些最佳实践可以帮助我们编写更加清晰、可维护的代码。</p><h3>组合式函数命名规范</h3><p>组合式函数的命名应该以"use"开头，这是一个社区广泛接受的约定。使用"use"前缀可以让其他开发者一眼就认出这是一个组合式函数，也方便 IDE 和工具识别。</p><pre><code>// 好的命名\nuseMousePosition\nuseLocalStorage\nuseDebounce\nuseApiClient\n\n// 不好的命名\nmousePosition\nLocalStorage\nDebounce\nApi\n</code></pre><h3>合理拆分组合式函数</h3><p>组合式函数应该保持单一职责原则。一个组合式函数应该专注于解决一个问题或实现一个特定的功能。如果一个组合式函数变得过于复杂，应该考虑拆分成多个更小的组合式函数。</p><pre><code>// 好的实践：职责分离\nconst { x, y } = useMousePosition()\nconst { data, isLoading } = useFetch(url)\nconst { value: debouncedValue } = useDebounce(value, 300)\n\n// 不好的实践：一个函数做太多事\nconst { user, posts, comments, fetchAll, updateUser, addPost } = useUserWithPosts()\n</code></pre><h3>响应式数据的响应性丢失问题</h3><p>在解构 reactive 对象时，如果直接解构，基本类型的属性会丢失响应性。应该使用 toRefs() 来保持响应性，或者将解构后的值赋给 ref。</p><pre><code>import { reactive, toRef, toRefs } from 'vue'\n\nconst state = reactive({ count: 0, name: '张三' })\n\n// 错误：count 和 name 会失去响应性\nconst { count, name } = state\n\n// 正确做法1：使用 toRefs\nconst { count, name } = toRefs(state)\n\n// 正确做法2：使用 toRef 转换特定属性\nconst count = toRef(state, 'count')\n</code></pre><h3>避免箭头函数替代生命周期钩子</h3><p>虽然技术上可以在 setup() 中直接调用生命周期钩子，但为了代码清晰，建议将相关逻辑分组放置。一种常见的做法是将生命周期钩子调用集中在一起，而不是分散在 setup() 函数的不同位置。</p><pre><code>export default {\n  setup() {\n    // 1. 初始化响应式数据\n    const data = ref(null)\n    \n    // 2. 定义方法\n    const fetchData = async () => { /* ... */ }\n    \n    // 3. 生命周期钩子\n    onMounted(() => {\n      fetchData()\n    })\n    \n    // 4. 返回模板需要的属性和方法\n    return { data, fetchData }\n  }\n}\n</code></pre><h3>setup() 中的 async 函数</h3><p>setup() 函数可以是异步的，这在需要使用 await 获取数据的场景中非常有用。但需要注意，当 setup() 返回一个 Promise 时，组件的渲染会被延迟到 Promise resolve。</p><pre><code>export default {\n  async setup() {\n    const user = ref(null)\n    \n    // 等待数据加载\n    user.value = await fetchUser('/api/user')\n    \n    return { user }\n  }\n}\n</code></pre><p>Vue 3 的 Composition API 为开发者提供了更加强大和灵活的工具来组织组件逻辑。通过合理使用 ref、reactive、computed、watch 以及组合式函数，我们可以构建出更加模块化、可复用和易维护的 Vue 应用程序。随着对 Composition API 的深入理解和实践，你会发现它在处理复杂业务逻辑和实现代码复用方面的巨大优势。</p>"
}
